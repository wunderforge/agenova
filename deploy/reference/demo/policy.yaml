apiVersion: agenova.io/v1alpha1
kind: PolicyBundle
metadata:
  name: reference-default-deny
  version: "1"
spec:
  rules:
    - team: team-a
      action: claim.create
      project: payments
      templateRef: engineer
