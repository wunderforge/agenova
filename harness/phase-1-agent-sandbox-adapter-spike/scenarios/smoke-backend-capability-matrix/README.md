# Scenario: Backend Capability Matrix

This scenario protects the Phase 1 decision artifact.

Expected evidence:

- the backend capability matrix exists;
- it separates Agent Sandbox capabilities from Agenova-owned capabilities;
- it includes a `Needs verification` state for assumptions that are not yet proven;
- it does not require a real upstream Agent Sandbox installation yet.

