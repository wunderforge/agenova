apiVersion: extensions.agents.x-k8s.io/v1alpha1
kind: SandboxClaim
metadata:
  name: smoke-claim
  namespace: agent-sandbox-smoke
spec:
  sandboxTemplateRef:
    name: smoke-template
  warmpool: smoke-pool
