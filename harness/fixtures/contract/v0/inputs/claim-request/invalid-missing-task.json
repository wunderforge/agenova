{
  "apiVersion": "agenova.io/v1alpha1",
  "kind": "ClaimRequest",
  "metadata": {"name": "fix-payment-timeout"},
  "spec": {
    "templateRef": "engineer",
    "requestedAccess": {"tools": ["git.read"]},
    "runtime": {"profileRef": "standard-isolated", "timeout": "20m"}
  }
}
